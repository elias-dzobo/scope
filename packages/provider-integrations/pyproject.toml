[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "provider-integrations"
version = "0.1.0"
description = "External provider integrations — search, scraping, LLM clients"
requires-python = ">=3.13"
dependencies = []  # all deps managed at workspace root

[tool.hatch.build.targets.wheel]
packages = ["src/provider_integrations"]
